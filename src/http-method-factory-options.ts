/**
 * Represents options that can be massed to an HTTP method factory.
 */
export default interface IHttpMethodFactoryOptions {
    /**
     * Determines whether or not CORS is enabled for the method.
     */
    enableCors: boolean;
}
